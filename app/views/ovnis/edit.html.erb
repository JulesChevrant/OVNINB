<%= simple_form_for [@ovni] do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :details %>
  <%= f.submit "Edit your onvi", class: "btn btn-primary" %>
  <%= link_to ovni.name, ovni_path(ovni.id) %>
<% end %>
