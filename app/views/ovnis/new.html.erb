<%= simple_form_for [@ovni] do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :details %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Submit your ovni", class: "btn btn-primary" %>
<% end %>
